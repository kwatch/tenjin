args = -n 10000

all:
	python bench.py $(args)

clean:
	rm -rf bench_django.* bench_cheetah.* bench_tenjin.* bench_kid.* bench_myghty.* bench_genshi.* bench_mako.* bench_templetor.* bench_jinja2.*
	rm -rf mako_modules

